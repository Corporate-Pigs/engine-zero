#include "engine-zero/core/EngineOptions.h"

Engine::Options::Options(std::string windowTitle, int windowWidth, int windowHeight)
    : windowWidth(windowWidth), windowHeight(windowHeight), windowTitle(windowTitle) {}